#!/bin/zsh
#
# .aliases - Set whatever shell aliases you want.
#

# mask vi
alias vi=vim

# more ways to ls
alias ll='ls -lh'
alias la='ls -lAh'

# fix typos
alias cd..='cd ..'

# misc
alias cm=chezmoi
alias please=sudo
alias zshrc='$EDITOR $ZDOTDIR/.zshrc'
alias zdot='cd $ZDOTDIR'
